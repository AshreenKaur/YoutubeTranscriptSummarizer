## 🚀 Installation and Setup

### 1. Prerequisites

You need **Python 3.8+** and a **Gemini API Key**. A **YouTube Data API Key** is optional but highly recommended to enable the search functionality.

### 2. Create the Environment

Create and activate a new Python virtual environment:

```bash
python -m venv venv
source venv/bin/activate  # On Linux/macOS
.\venv\Scripts\activate   # On Windows



